package C.repositories;

import C.entities.PlaysListTrack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlayListTrackRepository extends JpaRepository<PlaysListTrack, Long> {
}
